<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!--
      kb-platform 统一日志配置
      CONSOLE : 纯文本格式, 人类可读 (保持与 Spring Boot 默认格式一致)
      FILE_JSON : JSON 格式, 供 Promtail → Loki 采集, 自动包含 MDC 字段
    -->

    <!-- ===== 控制台输出 (纯文本, 格式与 Spring Boot 默认完全一致) ===== -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- ===== JSON 文件输出 (供 Promtail 采集) ===== -->
    <appender name="FILE_JSON" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE:-logs/rag-service.json}</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE:-logs/rag-service.json}.%d{yyyy-MM-dd}</fileNamePattern>
            <maxHistory>7</maxHistory>
        </rollingPolicy>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"service":"rag-service"}</customFields>
            <fieldNames>
                <timestamp>ts</timestamp>
                <version>[ignore]</version>
                <levelValue>[ignore]</levelValue>
            </fieldNames>
        </encoder>
    </appender>

    <root level="${LOG_LEVEL:-INFO}">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="FILE_JSON"/>
    </root>
</configuration>
