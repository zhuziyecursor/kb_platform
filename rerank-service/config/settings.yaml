kb_rerank_service:
  # BGE-Reranker 模型配置
  reranker:
    model_name: "BAAI/bge-reranker-v2-m3"
    max_length: 512
    batch_size: 20
    # 本地模型路径（跳过 HuggingFace 下载）
    local_model_path: "/Users/apple/project/kb-platform/rerank-service/models/bge-reranker-v2-m3"

  # 服务端口（由 main.py 读取环境变量 RERANK_SERVICE_PORT 或使用默认值）
