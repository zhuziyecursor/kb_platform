apiVersion: 1

datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    editable: false
    jsonData:
      maxLines: 1000
      derivedFields:
        - matcherRegex: "trace_id=(tr-[a-f0-9-]+)"
          name: TraceID
          url: ""
